export type UserData = {
  email: string;
};

export type JobPostData = {
  email: string;
  jobTitle: string;
  organizationName: string;
  region: string;
  city: string;
  address: string;
  startDate: string;
  employmentType: string;
  paymentType: string;
  workHours: number;
  minPayValue: number;
  maxPayValue: number;
  description: string;
  language: string;
  vacancies: number;
};
